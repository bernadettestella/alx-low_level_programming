#include <stdio.h>

/**
  * main - this is the main function
  * Return: always zero
  */
int main(void)
{
	char str[100] = "Programming is like building a multilingual puzzle\n";

	puts(str);
	return (0);
}
